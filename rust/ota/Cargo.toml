[package]
name = "ota"
version = "0.1.0"
edition = "2021"

[dependencies]
ed25519-dalek = { version = "2", default-features = false, features = ["std"] }
sha2 = "0.10"
hex = "0.4"
serde_json = "1"

[dev-dependencies]
